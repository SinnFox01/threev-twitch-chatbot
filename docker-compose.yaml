version: "3.8"
services:
    bot:
        build: .
        restart: unless-stopped
        environment:
            - NODE_ENV=production
        volumes:
            - ./data:/app/data # For SQLite persistence
